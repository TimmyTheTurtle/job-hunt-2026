Target resume bullets (tailor into `sim_v1` resume):

- Implemented modular C++ components to model domain-specific simulation logic, emphasizing clear interfaces and testability to support iterative experimentation.
- Designed and maintained multithreaded processing pipelines for high-throughput data flows, focusing on safe concurrency and predictable latency.
- Reduced end-to-end debugging time by introducing structured logging and lightweight observability hooks into simulation code and tooling.
- Built deployment and CI workflows for research and prototype services, enabling reproducible experiment runs in constrained production-like environments.
- Translated complex configuration and business rules into deterministic code paths and validation checks used by operational teams.
- Integrated simulation outputs with downstream tooling and visualization pipelines to speed iteration on model validity and performance tuning.

Notes: Keep bullets concrete and conservative; insert specific project names only if they are truthful and relevant. Omit fabricated metrics. Use `sim_v1` resume version when applying.
